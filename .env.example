# Public frontend URL used for canonical URLs, sitemap, robots and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Backend API URL baked into the frontend image at build time.
NEXT_PUBLIC_API_URL=http://localhost:8080/api
